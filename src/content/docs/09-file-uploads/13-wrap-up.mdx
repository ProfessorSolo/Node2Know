---
title: Wrap-Up
sidebar:
  order: 13
  label: Wrap-Up
---

import { LinkButton } from "@astrojs/starlight/components";

## What We Actually Built

You now have successfully navigated the complexities of processing binary document streams! In this chapter, we have built:

- **Referenced relationships** (`categoryId` + `populate()`)
- **Embedded data structures** (`tags` strings and `projectImages` objects)
- **Uploads working end-to-end** (Form → Multer Middleware → Disk Storage → MongoDB Metadata → EJS Views)

### ⏭ Next: Auth + Admin Lockdown

Because now your app has power tools... and we don't leave those lying around. Next up, we lock down our endpoints!
